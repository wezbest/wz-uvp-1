[project]
name = "lib1"
version = "0.0.2"
description = "bootyDancers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=13.9.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
